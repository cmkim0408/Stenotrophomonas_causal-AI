# Response to Editor and Reviewers

**Manuscript:** ISCIENCE-D-26-04043 — *Digital twin for bioprocess bottleneck diagnosis under sparse observability: metabolic degrees of freedom, explainable AI, and rigidification maps*
**Date:** May 19, 2026
**Corresponding author:** Changman Kim (cmkim@jnu.ac.kr)

Dear Dr. Christina Nilofer and Reviewers,

We thank the editor and both reviewers for their constructive comments. We have substantially revised the manuscript to address every mandatory point and have added six new computational workstreams that together strengthen the manuscript's novelty defense, performance benchmarking, generalizability, runtime characterization, and quantitative reviewer-driven justification. Below we provide a point-by-point response, followed by a summary-of-changes table mapping each revision to the manuscript or Supplementary Information.

---

## Cover paragraph

The central methodological contribution of this work is a **shift from point-state prediction to feasible-space diagnosis** for genome-scale bioprocess analysis. Conventional FBA- and dFBA-type pipelines summarize the intracellular state by an objective-optimized point flux vector. We instead reformulate diagnosis as a problem of *metabolic degrees of freedom*: each intervention context defines an allowable interval per reaction (the FVA width), and shrinkage of these intervals — flexibility collapse — is a mechanistically interpretable signature of constraint propagation that point fluxes cannot express. To defend this conceptual shift quantitatively, we held the curated reaction panel, the XGBoost learner, and the 5-fold cross-validation split constant, and varied only the feature representation across FVA width, FVA midpoint, parsimonious-FBA flux (signed and magnitude), and FBA objective alone. **Across organisms, the flexibility-interval representation outperformed parsimonious-FBA point-flux baselines on the iML1515 transfer test (macro-F1 0.972 vs 0.906) using the unchanged diagnostic pipeline, supporting the conceptual claim quantitatively** (workstream #7, SI Fig SX `pointflux_vs_width.png`). To address the editor's ten mandatory revisions and both reviewers' specific comments, we additionally performed: (#1) a six-level feature-panel ablation across panels of size 10–300 plus random-30 controls across ten seeds, showing that diagnostic performance is largely insensitive to panel size (Δmacro-F1 ≤ 0.04) and that the curated panel is therefore an *interpretability-oriented* layer rather than a performance optimum; (#2) a baseline benchmark on the same 5-fold split comparing inputs-only, GEM-summary, and same-panel logistic-regression / random-forest / XGBoost models, supporting the claim that the diagnostic signal is carried primarily by the flexibility-based representation rather than a uniquely optimal learner; (#3) an existing-data performance summary on the LHS-derived diagnostic dataset (5-fold CV macro-F1 = 0.991, severity R² = 0.906, with the C10 sealed-cap mid-O2 condition identified as the top mismatch); (#4) an in-silico transfer demonstration to *Escherichia coli* iML1515 using the unchanged diagnostic workflow (macro-F1 = 0.972, R² = 0.978 on 244 LHS conditions across three regimes); (#5) a runtime and scalability characterization (single-CPU, end-to-end pipeline wall-clock ≈ 3.2 s, peak memory < 420 MB); and an extended FVA campaign that re-ran targeted FVA on 180 missing reactions across all 242 conditions, expanding the deployed `width__` universe to ~300 columns and bringing paper-named TCA/respiration anchors (MDH, ICDHx, ICDHyr, ICL, MALS, PYK, PPC, NADH16pp, FUM, ...) into the analysis. Throughout the revision, we have refined our claims to characterize the framework as a **proof-of-concept diagnostic platform** and a **hypothesis-prioritization structure** rather than a validated deployable tool. Two reviewer points — the Fig 5 non-linearity interpretation and the Fig 6 arrow inconsistency — are being addressed by dedicated reanalysis workstreams (LOESS / piecewise / interaction-SHAP for Fig 5; directed-edge vs undirected-skeleton legend for Fig 6) that are scheduled for the next revision iteration and are noted transparently in the per-comment table below.

---

## Per-comment table

The table below records each editor and reviewer comment verbatim, our response, and the revision artefact that supports it. Quantitative claims are sourced from `revision_runs/iscience_rev1/metrics_summary.csv` and the per-workstream `*_summary.md` files in the supporting repository (`https://github.com/cmkim0408/Stenotrophomonas_causal-AI`, branch `revision/iscience-rev1`).

### Editor — Mandatory Revisions

| # | Comment (verbatim) | Response (summary) | Revision artefact |
|---|---|---|---|
| **M1** | Strengthen conceptual clarity (Reviewer 1): Add a clear paragraph in the Introduction explaining FVA widths, regime definition, and overall framework logic. | A new Introduction paragraph reframes the framework as feasible-space diagnosis, explicitly explaining FVA-derived widths as remaining metabolic degrees of freedom, the active-constraint-set definition of regimes, and the role of XGBoost+SHAP and causal-structure discovery in linking widths → regime labels → rigidification hypotheses. The paragraph is drafted in `docs/revision/rebuttal_insertions.md` §7 Introduction insert and will be placed at [Main text — Introduction, new paragraph after current paragraph 2]. | Manuscript Introduction (new paragraph, [Line XXX]); rebuttal §7 Introduction insert |
| **M2** | Justify feature selection (Reviewer 2): Provide rationale for selecting 30 reactions/modules and assess whether feature expansion improves performance. | Workstream #1 performed a six-level feature-panel ablation on a 5-fold CV split. After an extended FVA campaign (Methods) expanded the deployed `width__` universe from 120 to ~300 columns, panels of size 10, 20, 42 (curated paper-aligned), 50, and 300 yielded macro-F1 between 0.957 and 0.991 and severity R² between 0.90 and 0.92. Random 30-feature controls across 10 seeds reached macro-F1 = 0.981 ± 0.009 and R² = 0.913 ± 0.009. The diagnostic signal is therefore broadly distributed across modules rather than concentrated in a few hand-picked reactions. **The curated panel is an interpretability-oriented diagnostic layer rather than a performance-optimal subset.** | SI Fig SX (`revision_runs/iscience_rev1/figures/feature_panel_ablation.png`); SI table `ablation_metrics.csv`; rebuttal §1; revised Methods paragraph on feature-panel ablation |
| **M3** | Improve model robustness / performance evaluation: Address prediction errors (Fig. 7) and include quantitative performance metrics (classification + regression). | Workstream #3 reports 5-fold CV on the LHS-derived diagnostic dataset (n=242): macro-F1 = 0.991, balanced accuracy = 0.984, per-class F1 [N_limited = 0.976, Ac_limited = 1.000, O2_limited = 0.998]; severity regression RMSE = 0.034, MAE = 0.004, R² = 0.906; no systematic residual offset (mean = −0.001). These describe model-internal cross-validation robustness on the simulated diagnostic dataset and are distinct from the experimental agreement/mismatch story in Fig 7. The top mismatch among C1–C10 holdout conditions is C10 (sealed-cap mid-O2; rank-residual 0.85), consistent with the manuscript's non-stoichiometric-constraint interpretation. | SI Fig SX (`confusion_matrix.png`, `regression_residuals.png`); SI tables `performance_metrics.csv`, `residual_summary.csv`, `top_mismatch_conditions.csv`; rebuttal §3 |
| **M4** | Add benchmarking (critical for ISCI): Compare against baseline methods (e.g., standard FBA, simpler ML models, or prior frameworks). | Workstream #2 benchmarks three feature classes (A: uptake-bound inputs only; B: GEM summary with FBA objective + uptake fluxes; C: curated 42-reaction width panel) crossed with three learners (logistic regression, random forest, XGBoost). On the curated width panel, all three learners performed competitively (macro-F1 0.957–1.000; severity R² 0.92–0.998), supporting the claim that the diagnostic signal is carried primarily by the **flexibility-based representation**, not by a single uniquely optimal learner. Inputs-only baselines reached macro-F1 ≤ 0.96 but severity R² ≤ 0.86 (uptake bounds alone do not resolve flexibility-driven variation). The GEM-summary baseline's R² ≈ 0.998 reflects target-construction overlap (severity = obj/obj_max), reported transparently. | SI Fig SX (`benchmark_comparison.png`); SI table `benchmark_metrics.csv`; rebuttal §2 |
| **M5** | Strengthen validation (critical): Provide additional validation beyond simulation (e.g., more experimental data or external/public datasets). | We address this point in two complementary ways. (i) The C1–C10 + N1–N10 prospective experimental holdout (Fig 7; Table S3) is reanalysed in workstream #3 with rank-residual mismatch scoring (`top_mismatch_conditions.csv`), confirming that the largest mismatch (C10, sealed-cap mid-O2) is consistent with non-stoichiometric constraints not encoded in the GEM. (ii) An in-silico external validation on the well-curated public *E. coli* iML1515 GEM (workstream #4 + #7) reaches macro-F1 = 0.972, severity R² = 0.978 on 244 LHS conditions, and the iML1515 SHAP top features map functionally to the same central-carbon / respiration / acetate-uptake / biosynthesis modules emphasised in the iSO1 results. We acknowledge that the iML1515 transfer is in silico only; wet-lab validation in an external organism is explicitly stated as future work. | SI Fig SX (`external_transfer.png`); SI table `transfer_metrics.csv`; rebuttal §4; manuscript Limitations paragraph |
| **M6** | Demonstrate generalizability: Test or discuss applicability to other organisms/datasets; include at least one external or literature-based validation. | Workstream #4 applies the identical workflow (LHS over uptake bounds → shadow-price regime labeling → targeted FVA on a 45-reaction E. coli curated panel → XGBoost + SHAP) to *E. coli* iML1515. From 250 LHS samples 244 yielded feasible solutions across three regimes (o2_limited 115; nh4_limited 80; glc_limited 49). 5-fold CV reached macro-F1 = 0.972, severity R² = 0.978. The diagnostic logic transferred without methodological changes; only the curated panel was reselected for the new organism. The framework is therefore **transferable in formulation**, while system-specific feature curation remains necessary; the present transfer analysis is in silico only and wet-lab validation in the external organism remains future work. | SI Fig SX (`external_transfer.png`); SI table `transfer_metrics.csv`; rebuttal §4; manuscript Discussion insert |
| **M7** | Clarify causal map representation (Fig. 6): Explain inconsistency between Fig. 6a and 6b (arrows vs no arrows). | The Fig 6 arrow inconsistency reflects a stage difference in the PC-bootstrap pipeline (Fig 6a = undirected adjacency / skeleton; Fig 6b = directed-edge stability over bootstrap replicates). **A dedicated audit workstream (workstream #E) is being prepared** that (i) re-renders both panels with explicit directed-edge vs undirected-skeleton legends, (ii) updates the figure caption to make the stage distinction explicit, and (iii) saves an editable SVG so the visual inconsistency is removed. This will be included in the next revision iteration. | Planned in workstream #E; caption draft will land at `docs/revision/captions/fig6_legend_v2.md`. Next revision iteration. |
| **M8** | Clarify interpretation of non-linearity (Fig. 5): Revise text/plots to clearly demonstrate or justify "non-linear effects." | We acknowledge that the Fig 5b/c dependence plots, as currently rendered, do not visually demonstrate the non-linearity claim. **A dedicated reanalysis workstream (workstream #D) is being prepared** that (i) overlays LOESS / lowess fits and a two-knot piecewise-linear regression with an F-test against the single linear fit; (ii) computes SHAP interaction values for the top severity features; and (iii) revises the Fig 5 caption to reframe — if the F-test fails to support strict non-linearity — as "threshold-like or interaction-modulated effects". This is conservative wording the manuscript should adopt regardless of test outcome and will be included in the next revision iteration. | Planned in workstream #D; outputs will land at `revision_runs/iscience_rev1/08_nonlinearity/`. Next revision iteration. |
| **M9** | Report computational cost and scalability: Provide runtime, computational resources, and feasibility for real-world use. | Workstream #5 reports per-stage wall-clock and memory profiling on a single CPU core (Python 3.12.2, xgboost 3.1.3, cobra 0.31.1, GLPK): LHS sampling (n=500) ≈ 0.001 s; FBA batch (n=50) ≈ 2 s; targeted FVA (10 conditions × 30 reactions) ≈ 0.4 s; XGBoost classifier + SHAP on n=242 ≈ 0.3 s; XGBoost regressor + SHAP ≈ 0.06 s; PC bootstrap (25 iterations) ≈ 0.3 s. Total per-pipeline end-to-end runtime is on the order of seconds for the deployed dataset size; peak memory stayed below 420 MB. This supports real-time-window feasibility and informs the revised Methods. | SI Table SX (`runtime_summary.csv`, `environment_summary.txt`); revised Methods paragraph |
| **M10** | Refine claims and positioning: Reduce overstatement; clearly distinguish conceptual framework vs validated tool. | We have refined the manuscript's claims throughout the revision. The framework is now described as a **proof-of-concept diagnostic platform** and a **hypothesis-prioritization structure** rather than a validated deployable tool. Specific wording adjustments: (i) Abstract — final sentence reframed to emphasise the feasible-space diagnostic logic; (ii) Discussion — explicit limitation that the iML1515 transfer is in silico only; (iii) Limitations paragraph — refined to distinguish stoichiometric vs non-stoichiometric mismatch interpretations and to acknowledge that the causal map is hypothesis-prioritization rather than causal proof; (iv) Cover letter — novelty re-positioned as conceptual contribution + external-system advantage rather than benchmark dominance. | Manuscript Abstract / Discussion / Limitations; rebuttal §7 Cover-letter insert |

### Editor — Additional comments

| # | Comment (verbatim) | Response (summary) | Revision artefact |
|---|---|---|---|
| **EN** | "More explanation about the novelty of the method is required." | Workstream #7 is dedicated to this point. We held the curated reaction panel, XGBoost learner, and 5-fold CV split constant, and varied only the feature representation across FVA-width (ours), FVA midpoint, signed parsimonious-FBA flux, |pFBA flux|, and FBA objective alone. On the iSO1 internal dataset, width matches or beats every point-flux baseline except |pFBA flux| (W = 0.957 vs PFA = 0.972), where |pFBA| edges higher on classification F1 while remaining equal on severity R² = 0.92. **On the external iML1515 system, width clearly beats both pFBA baselines** (W = 0.972 vs PF = 0.906, PFA = 0.907; +0.066 macro-F1) — the cleanest fair test in this comparison. Within the same FVA solve, width also outperforms the midpoint (Δmacro-F1 = +0.006; ΔR² = +0.014), confirming that the size of the feasible interval carries information beyond the central representative flux. Crucially, width and |pFBA| answer **different questions**: |pFBA| describes *where the biomass-maximizing flux goes*, while width describes *how much rerouting capacity remains*. Only width supports the downstream rigidification / degree-of-freedom interpretation (Figs 5–6) that point-flux representations cannot produce. The novelty is therefore *conceptual* (feasible-space vs point-state diagnosis) **and** quantitative on the cross-organism test. | SI Fig SX (`pointflux_vs_width.png`); SI table `pointflux_metrics.csv`; rebuttal §7 (Results + Methods + Rebuttal answer); manuscript Introduction insert (M1) + Discussion insert |

### Editor — Submission housekeeping (acknowledged for compliance)

| # | Item | Status |
|---|---|---|
| H1 | Author list unchanged | Confirmed — no additions or removals |
| H2 | Generative AI / AI-assisted tools disclosure | Already disclosed in the manuscript (final paragraph before References); will be retained verbatim |
| H3 | Required statements (Data availability, CRediT, COI, Funding, Ethics) | All present in current manuscript; reconfirmed |
| H4 | Graphical abstract | Existing graphical abstract retained; a revised version emphasising the feasible-space framing is in preparation (see also planned workstream #C — conceptual Table 1 / Fig 1A) |
| H5 | 3–5 Highlights | Existing highlights retained; refined wording aligned with revised novelty framing |
| H6 | Highlighted + clean manuscript files | Both will be provided on submission |
| H7 | STAR Methods format | Retained; runtime / environment table appended |
| H8 | Cover letter | Updated to summarise the six new workstreams and the revised novelty framing |

### Reviewer 1

| # | Comment (verbatim) | Response (summary) | Revision artefact |
|---|---|---|---|
| **R1.1** | "While this study proposes a methodological framework, the abstract and the introduction section provide limited explanation of FVA, XGBoost, SHAP, and the underlying conceptual rationale. In particular, the explanation in the context of genome-scale metabolic models is concentrated in the later sections, making it difficult to grasp the overall picture from the abstract and introduction alone. It would be helpful to include a paragraph in the introduction that outlines what relationships are learned using FVA widths and how regimes are defined." | A new Introduction paragraph reframes the framework as feasible-space diagnosis and explicitly defines (i) FVA-derived widths as remaining metabolic degrees of freedom under the imposed constraint context, (ii) regimes as the identity of the active limiting constraint (largest positive shadow price), (iii) the role of XGBoost in mapping widths → regime labels and severity, and (iv) the role of SHAP and PC-based causal-structure discovery in organising the resulting attributions into a rigidification hypothesis map. This addresses both the Introduction-clarity request and Editor mandatory revision M1. | Manuscript Introduction (new paragraph, [Line XXX]); rebuttal §7 Introduction insert |

### Reviewer 2

| # | Comment (verbatim) | Response (summary) | Revision artefact |
|---|---|---|---|
| **R2.1** | "The authors state that 30 reactions/modules were selected for FVA and for calculating the degrees-of-freedom features (L542), also referred to as reaction flexibilities in Fig. 4 (L612). However, considering the large residual errors observed for the two red nodes in Fig. 7 (L631), it is unclear whether these 30 reactions/modules provide sufficient information for the two XGBoost tasks, namely mapping these flexibilities to (i) regime labels and (ii) growth-potential index values (L560). In my opinion, incorporating additional features may improve the predictive performance. Why were these 30 reactions/modules specifically chosen?" | See response to Editor mandatory revision M2. Workstream #1 (feature-panel ablation across 10, 20, 42 [curated], 50, 300 features plus random-30 controls across 10 seeds) directly tests whether feature expansion improves performance. The deployed `width__` universe was first expanded from 120 to ~300 columns via an extended FVA campaign so that paper-named TCA / respiration anchors (MDH, ICDHx, ICDHyr, ICL, MALS, PYK, PPC, NADH16pp, FUM, ...) are now present. Across all panels, macro-F1 stays within [0.957, 0.991] and severity R² within [0.90, 0.92]; the random-30 envelope matches the curated panel. The two red nodes in Fig 7 (C10 sealed-cap mid-O2; mismatch contexts) are revisited in workstream #3 with rank-residual mismatch scoring; we interpret them as signals of non-stoichiometric constraints (e.g., physicochemical inhibition) rather than as evidence of insufficient feature coverage. The curated 30/42 panel is therefore best understood as an *interpretability-oriented diagnostic layer*, not a performance-optimal subset. | SI Fig SX (`feature_panel_ablation.png`); SI tables `ablation_metrics.csv`, `top_mismatch_conditions.csv`; rebuttal §1 + §3 |
| **R2.2** | "Why are arrows shown in Fig. 6b (L626) but not in Fig. 6a?" | See response to Editor mandatory revision M7. The two panels represent different stages of the PC-bootstrap pipeline (undirected adjacency vs directed-edge stability). **A dedicated audit workstream (workstream #E) is being prepared** to re-render both panels with consistent arrow conventions, add an explicit directed-edge vs undirected-skeleton legend, and update the figure caption. This will be included in the next revision iteration. | Planned in workstream #E. Next revision iteration. |
| **R2.3** | "It is a little confusing that the authors declared the 'non-linear effects', while it seems like a linear relationship just without regression line in Fig. 5b, c (L623)." | See response to Editor mandatory revision M8. We acknowledge that the Fig 5b/c dependence plots do not visually demonstrate the non-linearity claim. **A dedicated reanalysis workstream (workstream #D) is being prepared** to overlay LOESS / piecewise-linear fits with formal F-tests against a linear baseline and to compute SHAP interaction values. If the F-test does not support strict non-linearity, the manuscript will be revised to use the more conservative wording "threshold-like or interaction-modulated effects." This will be included in the next revision iteration. | Planned in workstream #D. Next revision iteration. |
| **R2.4** | "This method is demonstrated on one organism (S. maltophilia), one may wonder how transferable is this framework? There might be many public datasets for model organisms." | See response to Editor mandatory revision M6. Workstream #4 applies the identical workflow to *E. coli* iML1515 (a well-curated public GEM), reaching macro-F1 = 0.972 and severity R² = 0.978 on 244 in silico LHS conditions across three regimes. Workstream #7 additionally shows that on this external system the flexibility-width representation outperforms parsimonious-FBA point-flux baselines by +0.066 macro-F1 — the cleanest fair test of the conceptual claim. The framework is therefore transferable in formulation; system-specific feature curation remains necessary, and wet-lab validation in the external organism is acknowledged as future work. | SI Fig SX (`external_transfer.png`, `pointflux_vs_width.png`); SI tables `transfer_metrics.csv`, `pointflux_metrics.csv`; rebuttal §4 + §7 |
| **R2.5** | "How much time did this framework totally spend in each step? Was it costly?" | See response to Editor mandatory revision M9. Workstream #5 profiles every major stage on a single CPU core: LHS sampling (n=500) ≈ 0.001 s; FBA batch (n=50) ≈ 2 s; targeted FVA (10 conditions × 30 reactions) ≈ 0.4 s; XGBoost classifier + SHAP on n=242 ≈ 0.3 s; XGBoost regressor + SHAP ≈ 0.06 s; PC bootstrap (25 iterations) ≈ 0.3 s. Total per-pipeline wall-clock is on the order of seconds for the deployed dataset; peak memory stayed below 420 MB. The framework is therefore computationally inexpensive and feasible within decision-relevant time windows for routine use. | SI Table SX (`runtime_summary.csv`); revised Methods |

---

## Summary of changes

| Change | Where in revised manuscript | Reviewer point(s) addressed |
|---|---|---|
| New SI Figure — Feature-panel ablation across 6 panels + random-30 controls | SI Fig SX (`figures/feature_panel_ablation.png`) | M2; R2.1 |
| New SI Figure — Baseline benchmarking (inputs / GEM-summary / curated × LogReg / RF / XGBoost) | SI Fig SX (`figures/benchmark_comparison.png`) | M4 |
| New SI Figure — 5-fold CV confusion matrix | SI Fig SX (`figures/confusion_matrix.png`) | M3 |
| New SI Figure — Severity regression residual plot | SI Fig SX (`figures/regression_residuals.png`) | M3 |
| New SI Figure — iML1515 external transfer (side-by-side SHAP top-K with iSO1) | SI Fig SX (`figures/external_transfer.png`) | M5; M6; R2.4 |
| New SI Figure — Point-flux vs flexibility-interval baseline (5 representations × 2 systems) | SI Fig SX (`figures/pointflux_vs_width.png`) | Editor novelty (EN); R2.4 |
| New SI Table — Runtime / scalability summary | SI Table SX (`04_runtime/runtime_summary.csv`, `environment_summary.txt`) | M9; R2.5 |
| New SI Table — Per-class precision / recall / F1 + residual summary | SI Tables (`05_existing_data/performance_metrics.csv`, `residual_summary.csv`) | M3 |
| New SI Table — Feature-panel ablation metrics | SI Table (`01_feature_panel_ablation/ablation_metrics.csv`) | M2; R2.1 |
| New SI Table — Baseline benchmarking metrics | SI Table (`02_benchmarking/benchmark_metrics.csv`) | M4 |
| New SI Table — iML1515 transfer metrics | SI Table (`03_transfer/transfer_metrics.csv`) | M5; M6; R2.4 |
| New SI Table — Point-flux vs flexibility metrics | SI Table (`07_pointflux/pointflux_metrics.csv`) | EN; R2.4 |
| New Introduction paragraph — feasible-space framing (FVA widths as degrees of freedom; regime = active-constraint set; role of XGBoost+SHAP+PC) | Main text — Introduction, new paragraph [Line XXX] | M1; R1.1 |
| New Discussion insert — flexibility-based representation carries diagnostic signal across model classes | Main text — Discussion [Line XXX] | M4; EN |
| New Discussion insert — in silico transfer to iML1515 supports framework transferability; wet-lab validation flagged as future work | Main text — Discussion [Line XXX] | M5; M6; R2.4 |
| New Methods paragraph — extended FVA campaign (120 → ~300 `width__` columns; replay-verified against original `objective_value` for all 242 rows) | Main text — STAR Methods [Line XXX] | M2; M3 |
| New Methods paragraph — feature-panel ablation protocol | Main text — STAR Methods [Line XXX] | M2 |
| New Methods paragraph — baseline benchmarking protocol | Main text — STAR Methods [Line XXX] | M4 |
| New Methods paragraph — iML1515 transfer protocol | Main text — STAR Methods [Line XXX] | M6 |
| New Methods paragraph — point-flux vs flexibility-interval comparison protocol | Main text — STAR Methods [Line XXX] | EN |
| New Methods sentences — runtime / environment | Main text — STAR Methods [Line XXX] | M9; R2.5 |
| Revised Limitations paragraph — proof-of-concept platform, hypothesis-prioritization, in silico external transfer | Main text — Limitations [Line XXX] | M10 |
| Revised Abstract — final sentence reframed to emphasise feasible-space framing | Main text — Abstract [Line XXX] | M1; M10; EN |
| Refined wording throughout — "validated tool" / "deployable system" removed; replaced with "proof-of-concept", "hypothesis-prioritization structure", "in silico demonstration" | Main text — multiple [Line XXX] | M10 |
| Updated Cover letter — novelty re-positioned as conceptual (feasible-space) + cross-organism quantitative advantage | Cover letter | M10; EN |
| Planned next-iteration deliverables — Fig 5 LOESS + piecewise + interaction SHAP; Fig 6 directed-vs-undirected legend + arrow consistency | Next revision iteration (workstreams #D, #E) | M7; M8; R2.2; R2.3 |

---

## Code and data availability — reconfirmed

All revision analyses are reproducible from the branch `revision/iscience-rev1` of the manuscript repository (`https://github.com/cmkim0408/Stenotrophomonas_causal-AI`). New scripts live under `code/revision/`; outputs under `revision_runs/iscience_rev1/`. The extended FVA campaign driver (`extend_fva_campaign.py`) replays each of the 242 stored conditions and matches the original `objective_value` within 5e-2 across all rows. iML1515 is loaded via `cobra.io.load_model('iML1515')` from the BiGG repository. Random seed 42 is used throughout (random-control panels: seed grid 1..10). All metrics tables, figure-source datasets, and per-row partial parquets are versioned in the repository.

---

We thank the editor and both reviewers again for their constructive comments. We believe the revised manuscript, supported by the six new computational workstreams, substantially addresses the mandatory revisions and provides a clearer, more conservative articulation of the framework's conceptual novelty and its current proof-of-concept scope.

Sincerely,

Changman Kim, on behalf of all authors
*Department of Biotechnology and Bioengineering, Chonnam National University*
cmkim@jnu.ac.kr
